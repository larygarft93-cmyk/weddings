/* Общее пространство имён сайта — сюда остальные модули
   кладут свои функции (setLanguage, renderMiniCalendar и т.д.),
   чтобы не плодить глобальные переменные и не зависеть от
   ES-модулей (это обычные <script>, порядок подключения важен). */
window.WeddingSite = window.WeddingSite || {
  lang: 'kg'
};
